{
  "name": "lora-caption-generator",
  "version": "1.0.0",
  "description": "Web application for generating caption files for LoRA training using GPT-4 Turbo",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\""
  },
  "keywords": [
    "lora",
    "caption",
    "gpt-4",
    "image-tagging",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
